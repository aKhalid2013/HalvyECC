name = "verify"
description = "Verifies completed feature implementation against its approved spec. Read-only — never modifies code. Triggers: \"verify\", \"spec-verifier\", \"check implementation\", \"verify SPEC-NNN\"."
file = ".agent-skills/spec-verifier/SKILL.md"
